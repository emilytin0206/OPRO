# ----------------------------------------------------
# 專案設定
# ----------------------------------------------------
project:
  task_name: 'BBH-boolean_expressions'
  log_dir: './logs'

# ----------------------------------------------------
# Scorer Model
# ----------------------------------------------------
scorer_model:
  client_type: 'Ollama'
  model_name: 'llama2'
  api_url: 'http://localhost:11434/api/generate'
  temperature: 0.0
  max_output_tokens: 1024

# ----------------------------------------------------
# Optimizer Model
# ----------------------------------------------------
optimizer_model:
  client_type: 'Ollama'
  model_name: 'codellama'
  api_url: 'http://localhost:11434/api/generate'
  temperature: 0.5
  max_output_tokens: 2048

# ----------------------------------------------------
# OPRO 優化參數
# ----------------------------------------------------
optimization:
  num_iterations: 20           # 測試時建議先設小一點 (例如 20)
  num_evals_per_prompt: 3
  num_prompts_to_generate: 4
  max_num_instructions_in_prompt: 20
  
  # [修正] 路徑改為 prompt (單數) 以匹配您的資料夾結構
  meta_prompt_path: 'prompt/meta_prompt.txt'
  
  # [新增] 支援 Scorer 的格式調整
  instruction_pos: 'Q_begin'
  is_instruction_tuned: true
  
  # [新增] 支援 Optimizer 的錯誤驅動策略
  num_few_shot_questions: 3
  few_shot_selection_criteria: 'accumulative_most_frequent'